{
  "name": "meteor-schema-graphql-bridge-verso",
  "repository": "versolearning/meteor-schema-graphql-bridge",
  "version": "1.0.5",
  "description": "Simple Schema - GraphQL Schema Bridge",
  "author": "Phuc Nguyen <npm@phucnguyen.info>",
  "license": "MIT",
  "main": "lib/schema-graphql-bridge.js",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && ./node_modules/babel-cli/bin/babel.js -s inline -d lib src",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel": "^6.3.13",
    "babel-cli": "^6.3.15",
    "babel-preset-es2015": "^6.3.13"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "dependencies": {
    "simpl-schema": "^0.3.0"
  }
}
